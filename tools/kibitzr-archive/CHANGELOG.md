# Changelog

## 0.3.0 — unreleased

- Replace the single-file dashboard with an atomic static evidence browser.
- Add hash-verified document reconstruction, neutral before/after diffs,
  evidence pages, annotations, inert raw downloads, and proof-bound timestamp
  coverage.
- Separate archive integrity, current collection health, and timestamp
  coverage on the dashboard.
- Add an accessible persistent System default/Light/Dark selector using local
  CSS and JavaScript, with an operating-system fallback and light print output.
- Add generator-time link and machine-path validation.
- Add `publication-manifest.json` for derived-build provenance and aggregate
  publication counts.
- Document the private collector, explicit review, static export, and public
  host boundary for a possible future Vercel deployment.

The package now declares `0.3.0`; no `v0.3.0` tag has been created.
