# Control target pack

Copy this file to the host-only Kibitzr configuration and replace the invalid
placeholder URL. Set the same URL as `EVIDENCE_CONTROL_URL` and the check name
as `EVIDENCE_CONTROL_CHECK` in host configuration. A repository default must
not silently select an environment-specific service.
