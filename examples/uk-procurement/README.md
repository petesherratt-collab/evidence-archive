# UK procurement reference target pack

The reviewed reference configuration remains at
`trials/gov-contracts/kibitzr.yml`. Copy it into the host configuration area and
review every URL, selector, period, robots policy and placeholder before use.
It is an example target pack, not an engine default.

